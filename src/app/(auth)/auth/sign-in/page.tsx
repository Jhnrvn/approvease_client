// components
import SignInForm from "@/components/authentication/SignInForm";


// component that renders the sign-in page
const page = () => {
  return (
    <div className="w-200">
      <SignInForm />
    </div>
  );
};

export default page;
